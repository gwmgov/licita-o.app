/**
 * ÚNICO arquivo que você precisa editar ao publicar o frontend na nuvem.
 *
 * Depois que o backend estiver publicado (Railway, Render etc.), copie a URL
 * pública dele e cole abaixo, mantendo o "/api" no final. Exemplo:
 *
 * window.API_BASE_URL = "https://licitacoes-backend.up.railway.app/api";
 *
 * Se deixar como null, o sistema tenta usar automaticamente
 * "http://localhost:3000/api" (funciona apenas em instalação local).
 */
window.API_BASE_URL = "https://licita-o-app.onrender.com/api"
